# Cleaned-Company-Employee-Dataset
Cleaned a messy Company Employee Dataset using Python and Pandas by identifying missing values, duplicates, inconsistent entries, and incorrect data types. Applied median/mode imputation, formatting, datetime conversion, and duplicate removal. Verified the cleaned dataset and exported the final data as a CSV file.
